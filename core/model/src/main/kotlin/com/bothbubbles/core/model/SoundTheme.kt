package com.bothbubbles.core.model

/**
 * Available sound themes for message send/receive sounds.
 */
enum class SoundTheme(val displayName: String) {
    DEFAULT("Default"),
    HIGH("High"),
    POP("Pop")
}
